# AE 原生曲线版本

## 交互与兼容范围

Window → DalimaoCurves 命令打开 AE 自带的图形编辑器，并显示可同时操作 AE 的 Win32 工具窗口。再次执行命令会返回图层滑条。原来的 GDI+ 曲线绘制、像素标尺校准、鼠标移出关闭和阻塞消息循环已移除。

工具提供属性/两帧段落/维度选择、六个曲线样式按钮、两个影响滑块和模板库。选择属性时也请求 AE 选中对应属性，以便原生编辑器显示曲线。滑块只调整手柄长度；曲线速度、关键帧时间和值仍可在 AE 原生编辑器中直接调整。

当前适配本机 Windows AE 2025（25.4）英文界面。AE 官方将 Shift+F3 定义为图形编辑器与图层条的切换键：[Adobe 关键帧编辑说明](https://helpx.adobe.com/mena_en/after-effects/desktop/animate-in-after-effects/animation-keyframes/editing-moving-copying-keyframes.html)。该键需要保留给 AE；插件可以绑定 `4` 等其他键。插件检测到自身通过 Shift+F3 呼出时会提示更换绑定。

本机 UI Automation 暴露的时间轴名称为 `AE Timeline`，图形模式具有可见的 `Choose graph type and options` 和 `Auto-zoom graph height` 控件。当前实现依据这些状态，尚不声称支持其他界面语言。存在多个可见时间轴或读取状态不明确时停止切换并显示原因。

## 实现边界

- `DalimaoCurves.cpp`：Win32 自绘控件、快捷键接收、动作排队和 AEGP Idle/Command/Death 回调。窗口事件不调用 AE suites。
- `CurveStreams.inl`：逐次获取当前图层的数值属性。只在当前 AEGP 回调内保留 stream/value 资源，结束时释放。
- `CurvePresetPanel.inl`：模板持久化、端点缓动读写、撤销和失败恢复。文件名为兼容历史代码保留，已不包含自绘面板。
- `CurvePresetModel.h`：不依赖 AE 的曲线模板数学与序列化。
- `NativeGraphBridge.h`：独立 MTA 线程读 UI Automation、确认时间轴状态与焦点、执行原生切换并检查结果。不会在 AEGP 主线程中等待 UIA。

动作只携带合成 ID、图层 ID、当前 AE 会话的唯一属性 ID 和复制出的两帧数据。AE 忙碌时编辑控件暂时禁用；已经松开的滑块修改会先执行，然后处理关闭请求。应用前重新读取目标和端点；目标消失、时间/值/缓动变化或读取失败会停止旧操作并刷新。

当前版本静止时不再轮询 AE。更换图层或在原生编辑器中修改曲线后，点击“读取”更新；编辑前仍重新验证目标，若快照已过期则停止一次并刷新。

打开/关闭是异步请求。检测已处于目标模式时不发送按键；否则确认 AE 前台、时间轴焦点和修饰键释放，再发送一次 Shift+F3，随后检查模式。失败时保留工具窗口和错误提示，允许重试，不自动重复发送切换键。

## 验证

```powershell
py deploy.py --build-only
powershell -NoProfile -ExecutionPolicy Bypass -File tests/run-tests.ps1
py deploy.py --verify
```

自动测试覆盖预设数学、模板文件冲突/锁、端点与维度保留、撤销组、失败回滚、游走拒绝、滑块对零值差非零速度的保留以及过期关键帧快照拒绝。宿主内的焦点、键盘配置和界面状态需要单独实测。

手工验收：

1. 从图层条按自定义快捷键，确认出现原生图形编辑器和工具窗口；再次按键恢复图层条。
2. 预先打开原生图形编辑器，再呼出工具，确认曲线不会被关闭。
3. 在工具的按钮、属性下拉框和滑块获得焦点时，检查同一快捷键与 Esc。
4. 依次应用不对称、双侧最长预设；检查两个滑块、Ctrl+Z、模板保存和重开。
5. 修改原生曲线后等待同步再调整滑块；在 AE 更换图层或删除关键帧后确认不会误改旧对象。
6. 检查鼠标移出仍能继续操作 AE，长按快捷键不会来回开关。

## 本轮验证记录（2026-09-12）

- Release/x64 构建通过，0 错误、0 警告，DLL 为 130,560 字节。模型及模拟 AE 集成测试通过。
- 用户实际测试的旧安装文件为自绘版本（SHA-256 以 `006008d7` 开头），其关闭逻辑不会关闭 AE 原生图形编辑器。现在已替换为原生版本，安装/构建 SHA-256 一致：`a5d6d4202ab3868905028175847060caa2a85f2a8b73158c83e5839b56ff89c2`。
- 旧插件备份为安装目录下 `DalimaoCurves.aex.20260912-155348-842437.bak`。
- 已通过 AE 脚本接口将原有未保存工程另存为 `x64/Native-Graph-Validation.aep`；在其中添加独立的 `Dalimao Native Curve QA` 合成和两帧透明度属性。未覆盖根目录原始测试工程，也没有更改脚本安全设置。
- 宿主内已验证：原生曲线已经打开时呼出工具保持曲线；按 `4` 返回图层条；再按 `4` 打开原生曲线和工具；再次按 `4` 关闭两者。两次关闭均由工作线程日志和刷新后的界面树交叉确认。
- 控件检查确认六个曲线样式、模板保存/应用/删除/重载、属性/段落/维度选择和两个手柄长度滑块均存在；测试属性载入后处于可用状态，没有自绘曲线画布。
- 自动化鼠标点击仍返回 `coordinate input geometry is unavailable`，因此未声称完成宿主内按钮点击、滑块拖动和每种自定义按键的全面回归。数值修改、撤销、过期操作和文件持久化逻辑由集成测试覆盖，交互手感由用户试用确认。
- 新版标题为 `Dalimao Curves 2 · 原生曲线工具`，诊断日志位于 `%APPDATA%\DalimaoCurves\native-graph.log`，记录加载和每次原生切换的实际结果。
- 独立并发复核确认请求、结果发布及 DLL 工作线程生命周期的已知问题已修复。历史自绘版本记录不作为原生版本的宿主验收证据。

## 鼠标浮窗 UI 修订（2026-09-12）

按照后续反馈，恢复鼠标附近的无系统标题栏浮窗。移除的只有独立曲线画布，六个预设、属性/区间/维度选择、读取、双滑块和模板保存/应用/删除/重载均保留。右上角 × 与同一快捷键会关闭工具并请求 AE 返回图层滑条；拖动标题区域可移动浮窗，鼠标移出仍可操作 AE。

视觉参考 [Flow](https://aescripts.com/flow/) 的紧凑预设组织与 [EaseCopy](https://aescripts.com/easecopy/) 的缓动复用操作；采用 AE 深灰色调、蓝色滑块和清晰的选择/预设/手柄/模板分区。没有引入第三方脚本代码或自绘曲线编辑器。

`CurvePopupUI.inl` 负责 Win32 自绘按钮、下拉框外观、滑块绘制、DPI 尺寸及鼠标工作区定位。AE 操作仍由原有排队和 AEGP 回调执行，原生曲线状态桥接不变。

可通过 `powershell -NoProfile -ExecutionPolicy Bypass -File tests/preview-popup.ps1` 生成实际控件绘制预览（`x64/tests/popup-preview.png` 与 150% 版本）。预览程序还检查 100%、150%、200% DPI 以及带负坐标显示器的边缘定位。它是离屏绘制检查，不代替 AE 内的拖动和焦点验收。

本次安装与检查：Release/x64 0 错误、0 警告，模型和模拟 AE 集成测试全部通过；实际 Win32 离屏绘制、96→144 DPI 控件状态保留、长错误信息换行和边缘定位通过。安装 SHA-256 为 `a1b8dade83620b5a8f776f750ff6a35b4b7a3f1b24f8346deb186a257444a56c`，145,408 字节；旧安装已自动备份。

AE 宿主已确认新版“鼠标浮窗”存在、全部控件 ID 保留，`4` 呼出时保持已打开的原生曲线，第二次 `4` 关闭浮窗并返回图层滑条。当前桌面 175% 缩放下，日志记录鼠标 `(1505,1217)`，弹窗边界 `(1530,303,2314,1192)`，说明靠近下边缘时向上展开。宿主鼠标点击/滑块拖动仍由用户验证；离屏预览不是 AE 窗口截图。

## 性能与曲线缩略图修订（2026-09-12）

取消旧版每 500ms 的全属性树扫描、全关键帧读取和控件重建，也取消静止浮窗对 AE idle 间隔的 6ms 限制。只有排队操作和原生切换期间请求短间隔。打开/读取/实际编辑才访问属性，编辑前仍检查合成、图层和关键帧快照，避免写入过期对象；换图层后点读取。静止时不实时同步滑块：在 AE 修改曲线后，点击读取更新；如果直接编辑碰到过期快照，会停止一次并刷新。

没有显式选择属性时，自动选择当前图层第一个至少含两帧的数值属性；明确选中的可编辑属性优先。默认不会同时修改多个属性。AE 原生选择会与自动选择的属性同步。

六个预设按钮显示实际影响/归一化速度生成的曲线缩略图，模板选择框和每个下拉条目显示对应已保存模板的形状。图示是归一化的“时间—数值”缓动形状，不是 AE 速度图的截图；模板归一化速度、时长/值差换算和文件格式不变。缩略图采样上限 65 点，没有定时器、AE suites 或可拖动曲线画布。

回归测试新增：10000 次静止 idle 零 AE suite 请求且不改变 sleep；100 次相同组合框刷新只 reset/select 一次；自动属性选择覆盖无选中、多个候选、显式优先及不足两帧；缩略图验证线性、非对称影响、最长手柄、超调和保持插值。

安装验证：155,648 字节，SHA-256 `fcfd3592929afd0728e512d05b5895cf78ab009917ac4f7da7d7b3bcbfbe2797`，安装/构建一致，旧文件已备份。AE 脚本实测：`Dalimao Auto Selection QA` 含 Position/Scale/Opacity 三个两帧属性；呼出前 selectedLayers=1、selected leaf properties=0；呼出后 selectedLayers=1、selected leaf properties=1，实际选中 `Position (2 keys)`。此项验证 AE 的真实选择，不只检查浮窗提示文字。
正常启动流程再次验证：不使用脚本检查弹窗，只选图层后按 `4`，浮窗显示自动选择成功，原生桥日志确认 `Graph Editor opened`，UIA 树确认原生曲线标记出现。脚本检查弹窗曾使 AE 可访问性树暂时退化、状态读取失败；关闭弹窗后此次进程仍未恢复，正常保存/重启后恢复。没有把这次失败当作快捷键成功记录。

## 开关延迟修订（2026-09-12，后续反馈）

本轮针对打开/关闭路径：旧桥接在一次切换中反复从 AE 的完整 UIA 树查找时间轴。改为枚举可见的同进程原生窗口，再批量验证 UIA 身份；每个请求只解析一次时间轴。明确排除本插件自己注册的浮窗类及其子控件，其他 AE 自有窗口仍参与唯一性检查。快速路径不确定时保留完整搜索回退。每次状态证明都重新验证时间轴身份、可见性与原生图形模式标记；没有缓存 Graph Editor 开关状态。

浮窗正常关闭时立即隐藏并保留控件，下次在相同 owner/DPI 下复用。关闭后的隐藏控件不再刷新。打开时先完成属性读取并释放宿主资源，再请求原生切换，减少 AE 写操作和 UIA 检查重叠。切换失败时恢复浮窗并显示原因。窗口被意外销毁时清除 HWND 缓存；失败恢复时按需重建。插件卸载仍释放窗口与绘图资源。

日志新增 `Open UI timing`、`Open/Close confirmed` 以及每次原生请求的分段耗时。`controls` 是创建/复用控件阶段，`main` 是命令回调中的界面准备阶段，`confirmed` 从命令或关闭动作开始计时直到 AE Idle 回调确认结果，不包含用户按键送达回调之前的等待。原生请求的 discovery/state/focus/confirm 部分有重叠，不应直接相加。

实测环境为本机 AE 2025 25.4、175% 缩放、独立 QA 合成。只读外部探针对旧版完整搜索测得约 239–320ms，新窗口查找约 58–62ms；此项只对比查找方法，不等于完整开关耗时。安装后的最终版本实际记录：

| 操作 | 控件创建/复用 | 界面准备 main | 到原生模式确认 |
| --- | ---: | ---: | ---: |
| 首次打开 | 47ms | 78ms | 625ms |
| 第二次打开 | 0ms | 16ms | 391ms |
| 第三次打开 | 0ms | 15ms | 375ms |
| 第一次关闭 | 保留并隐藏 | — | 344ms |
| 第二次关闭 | 保留并隐藏 | — | 328ms |

以上三次打开、两次关闭均为 `resolves=1 native=1 fallback=0 polls=1`；日志结果和随后刷新的原生 UIA 模式标记一致。浮窗关闭时先隐藏，表内关闭时间是 AE 图层条确认时间。未测量旧版完整按键端到端耗时，因此不计算整体提速百分比。

最终 Release/x64 构建 0 警告、0 错误；模型和集成测试通过，新增实际 Win32 窗口复用、外部销毁及重建检查。只读探针为 `tests/native_graph_probe.cpp`，用 `tests/build-native-probe.ps1` 构建，传入实际 AE 主窗口 HWND；该探针不发送任何键盘输入。独立复核已检查时间轴重新验证、输入前焦点/前台/停止条件、窗口生命周期和自有窗口排除。

最终安装 164,864 字节，构建/安装 SHA-256 一致：`e32b1d5e202516a42997dcf1d6b5f2a400a9e9337adf63d83f9291d9986dc12e`。前一安装备份为 `DalimaoCurves.aex.20260912-172857-304124.bak`。工程已保存，AE 保持打开，最后停在原生曲线和鼠标浮窗，供用户继续验证。

限制：上述小型合成不能代表大型项目或 AE 忙碌时的延迟。首次冷打开仍有约 0.6 秒的完整确认时间。一次通过 shell 启动的 AE 会话未暴露原生 UIA 时间轴，桥接正确返回失败；通过正常桌面启动并重新打开工程后恢复，该启动环境差异未作根因裁决。尚未完成多时间轴、切换工作区、不同界面语言和用户快速连续敲键的宿主回归。
